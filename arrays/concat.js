const salaJs = ['Thiago', 'Ana', 'Caio', 'Lara', 'Marjorie', 'Leo'];
const salaPython = ['João', 'Marcia', 'Maria'];

const salasUnificadas = salaJs.concat(salaPython);

console.log(`salas unificadas: ${salasUnificadas}`);

// A função concat() cria um novo array que é a junção dos arrays originais, sem alterar os arrays originais.
// Ela pode receber vários arrays como parâmetros e os concatena em um único array.